import { UserContext } from 'src/helpers/types';

export const DEBUG_USER: UserContext = {
  email: 'admin@admin.com',
  id: 1,
  is_global_admin: true,
  username: 'admin',
  is_owner: true,
  is_sg: false,
};
